checkout:
  post:
    - npm install -g coffee-script
    - npm install -g wintersmith

deployment:
  production:
    branch: master
    commands:
      - git config user.email "ops@dobt.co"
      - git config user.name "CircleCI"
      - grunt deploy
