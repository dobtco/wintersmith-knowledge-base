extends layout
block content
  .main
    .landing
      h1 Don't worry, we've got you covered.
      include partials/search.jade
      +search("Search knowledge base articles...")
    .listings.grid
      include partials/toc.jade
      +toc(contents.articles._.directories, true)(class='item lap_two_columns')
