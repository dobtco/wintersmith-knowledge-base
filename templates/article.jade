extends layout
block content
  .grid
    .left_nav.item.lap_three_columns
      p
        | Choose an app:
        select#choose-app
          each dir in directories
            option(value=dir.filename, selected=(cur_dir == dir.filename))= caps(dir.filename.split('/')[1])
      include partials/toc.jade
      +toc(_.filter(directories, function(d) {if (cur_dir == d.filename) {return true}}), false)
      div
        b Still need help?
        .still_need_help
          a(href='#') Contact us
    .article.item.lap_nine_columns
      .breadcrumbs
        - var rs = locals.page.filename.split('/').slice(1)
        each r, i in rs
          if i < (rs.length-1)
            if r != 'index.html'
              = caps(format(r)) + ' '
              | &rarr;&nbsp;
        - var last = rs[rs.length-1]
      if last != 'index.html'
        h1= caps(format(rs[rs.length-1]))
      else
        h1= caps(format(rs[rs.length-2]))
      .grid
        .item.lap_six_columns
          h3 On This Page
          ol
            each h in page.metadata.onThisPage
              li: a(href!=h[1])= h[0]
        if page.metadata.FAQs.length > 0
          .item.lap_six_columns
            h3 FAQs
            ol
              each h in page.metadata.FAQs
                li: a(href!=h[1])= h[0]
      hr
      != page.html
